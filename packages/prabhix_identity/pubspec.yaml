name: prabhix_identity
description: Prabhix Identity OIDC client (PKCE, secure storage, refresh mutex, logout).
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_appauth: ^12.1.0
  flutter_secure_storage: ^9.2.4
  url_launcher: ^6.3.1
  uuid: ^4.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

